This docker-compose.yml file will create an nginx reverse proxy.
The proxy will route requests from port 8000 to the default ports for all 4 services.
This requires you to have all four services running locally (and on the default ports).